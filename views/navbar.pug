nav.navbar.navbar-expand-lg.navbar-dark.bg-dark.fixed-top
  a.navbar-brand(href='#') DoCreatness
  button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarNav', aria-controls='navbarNav', aria-expanded='false', aria-label='Toggle navigation')
    span.navbar-toggler-icon
  #navbarNav.collapse.navbar-collapse
    ul.navbar-nav
      li.nav-item
        a.nav-link(href='/home')
          | Home
          span.sr-only (current)
      li.nav-item
        a.nav-link(href='/games') Games
      li.nav-item
        a.nav-link(href='/movies') Movies
    if !currentUser
      ul.navbar-nav
        li.nav-item
          a.nav-link(href='/register') Sign Up
    else
      ul.navbar-nav
        li.nav-item
          a.nav-link(href='/profile') Profile
  if currentUser
    div
      a.btn.btn-light.pull-md-right(href='/logout') Log out
  else
    a.btn.btn-secondary.pull-md-right(href='login') Login
